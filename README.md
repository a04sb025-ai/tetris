# NEON BLOCKS

ブラウザだけで遊べる、ネオンスタイルのテトリスです。

## 起動方法

`index.html` をブラウザで直接開くか、ローカルサーバーを起動してください。

```bash
python3 -m http.server 8000
```

その後、`http://localhost:8000` にアクセスします。

## 操作

- `←` / `→`: 移動
- `↑`: 回転
- `↓`: ソフトドロップ
- `Space`: ハードドロップ
- `C`: ホールド

スマートフォンでは画面下部のタッチボタンで操作できます。
